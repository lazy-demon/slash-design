{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compose",
            "type": "shell",
            "presentation": {
                "reveal": "always"
            },
            "command": "cd slash_server && docker-compose up --build --detach"
        },
        {
            "label": "shutdown",
            "type": "shell",
            "presentation": {
                "reveal": "always"
            },
            "command": "cd slash_server && docker-compose stop"
        }
    ]
}